# stand-in for the real control module
"""
# represents a cognitive reset
class CognitiveReset:
    def __init__(self, reset_type, reset_time, reset_duration):
        self.reset_type = reset_type
        self.reset_time = reset_time
        self.reset_duration = reset_duration

    def __str__(self):
        return "CognitiveReset: " + str(self.reset_type) + " " + str(self.reset_time) + " " + str(self.reset_duration)

    def __repr__(self):
        return "CognitiveReset: " + str(self.reset_type) + " " + str(self.reset_time) + " " + str(self.reset_duration)
"""